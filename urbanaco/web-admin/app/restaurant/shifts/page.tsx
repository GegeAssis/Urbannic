'use client'; import {useEffect,useState} from 'react'; import api from '@/lib/api'; export default function Page(){ const [s,setS]=useState<any[]>([]); useEffect(()=>{api.get('/shifts').then(r=>setS(r.data))},[]); return(<div className='card'><h1>Shifts</h1><ul>{s.map(x=><li key={x.id}>{x.id} — {x.courierName} — {x.status} <a className='btn' href={`/restaurant/shifts/${x.id}/packages`}>Pacotes</a></li>)}</ul></div>)}
